<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chlhrssj.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.mvvm.detail.WebActivity"></activity>
        <activity android:name=".ui.mvvm.home.HomeActivity" />
        <activity android:name=".ui.livedata.SimpleLiveDataActivity" />
        <activity android:name=".ui.livedata.NoLiveDataActivity" />
        <activity android:name=".ui.viewmodel.SimpleVMActivity" />
        <activity android:name=".ui.viewmodel.NoVMActivity" />
        <activity android:name=".ui.lifecycle.LifecycleActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>